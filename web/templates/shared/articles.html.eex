<div class="card">
  <h4 class="card-header">Children</h4>
  <div class="card-block">
    <%= if @children == [] do %>
      <div class="text-xs-center text-muted">
        <p>This entry has no children attached to it.</p>
        <%= if @conn.assigns.user do %>
          <a href="<%= entry_path(@conn, :child, @entry_id) %>">
            <b>
              <i class="fa fa-plus" aria-hidden="true"></i> Add article
            </b>
          </a>
        <% end %>
      </div>
    <% else %>
      <div class="list-group">
        <%= for child <- @children do %>
          <a class="list-group-item" href="<%= entry_path(@conn, :show, child.id) %>">
            <%= render Artus.SharedView, "entry.html", %{entry: child} %>
          </a>
        <% end %>

        <%= if @conn.assigns.user do %>
          <a class="list-group-item text-xs-center" href="<%= entry_path(@conn, :child, @entry_id) %>">
            <b>
            <i class="fa fa-plus" aria-hidden="true"></i> Add article
            </b>
          </a>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
